<%= render('layouts/errors', :object => @response) %>

<%= form_for [@question, @response] do |f| %>
  <%= f.label :answer %>
  <%= f.text_field :answer %>
  <%= f.label :tag %>
  <%= f.text_field :tag %>
  <p><%= f.submit %></p>
<% end %>
